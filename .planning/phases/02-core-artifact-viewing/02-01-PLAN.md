---
phase: 02-core-artifact-viewing
plan: 01
type: execute
wave: 1
depends_on:
  - 01-02 (Phase 1 Foundation — IPC, theme, ViewerRegistry)
files_modified:
  - ai-cli-studio/src/main/ipc/index.ts (modify — register fs IPC)
  - ai-cli-studio/src/main/ipc/fs.ts (create — file system IPC handlers)
  - ai-cli-studio/src/main/index.ts (modify — add app.getPath for project root)
  - ai-cli-studio/src/preload/index.ts (modify — add fs methods to electronAPI)
  - ai-cli-studio/src/shared/types/ipc.ts (modify — add fs channel constants)
  - ai-cli-studio/src/shared/types/window.d.ts (modify — add fs methods to ElectronAPI)
  - ai-cli-studio/src/shared/validators/fs.ts (create — Zod schemas for fs IPC)
  - ai-cli-studio/src/shared/validators/fs.test.ts (create — contract tests for fs IPC)
  - ai-cli-studio/src/renderer/src/hooks/useElectronAPI.ts (modify — add fs hooks)
  - ai-cli-studio/src/renderer/src/stores/artifactTabs.ts (create — Zustand store)
  - ai-cli-studio/src/renderer/src/stores/uiState.ts (create — Zustand store)
  - ai-cli-studio/src/renderer/src/stores/fileBrowser.ts (create — Zustand store)
  - ai-cli-studio/src/renderer/src/stores/index.ts (create — barrel export)
  - ai-cli-studio/src/renderer/src/components/Layout.tsx (create — main app layout)
  - ai-cli-studio/src/renderer/src/components/TabBar.tsx (create — tab bar component)
  - ai-cli-studio/src/renderer/src/components/ViewModeControls.tsx (create — segmented control)
  - ai-cli-studio/src/renderer/src/components/ViewerPanel.tsx (create — content area with split pane)
  - ai-cli-studio/src/renderer/src/components/ResizeHandle.tsx (create — draggable split handle)
  - ai-cli-studio/src/renderer/src/App.tsx (modify — replace WelcomeScreen with Layout)
  - ai-cli-studio/src/renderer/src/main.tsx (modify — add store provider if needed)
  - ai-cli-studio/package.json (modify — add zustand, @dnd-kit/core, @dnd-kit/sortable)
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - ST-01

must_haves:
  truths:
    - "App launches with tabbed layout: sidebar placeholder + tab bar + viewer panel"
    - "Tab bar supports opening tabs (hard limit of 5), switching, closing via ×, and drag reorder"
    - "View mode segmented control shows 3 options: Preview, Code, Side-by-side"
    - "Global view mode applies to all tabs — switching mode changes all open tabs"
    - "Side-by-side mode shows preview and code panes with draggable resize handle at 50/50 default"
    - "When no tabs are open, Artifact Index placeholder is shown in viewer panel"
    - "Zustand stores manage artifact tabs, UI state, and file browser state"
    - "File browser store is defined but not yet wired to a UI component (Plan 02)"
    - "IPC channels fs:listDir, fs:readFile, fs:getFileInfo exist with Zod validation"
    - "Overflow tabs (beyond bar capacity) go into a dropdown (») menu"
  artifacts:
    - path: "src/main/ipc/fs.ts"
      provides: "File system IPC handlers (listDir, readFile, getFileInfo)"
      exports: ["registerFSIPC"]
    - path: "src/shared/validators/fs.ts"
      provides: "Zod schemas for file system IPC payloads"
      exports: ["FSListDirSchema", "FSReadFileSchema", "FSGetFileInfoSchema"]
    - path: "src/renderer/src/stores/artifactTabs.ts"
      provides: "Zustand store for open tabs, active tab, open/close/switch/reorder"
      exports: ["useArtifactTabsStore"]
    - path: "src/renderer/src/stores/uiState.ts"
      provides: "Zustand store for view mode and split position"
      exports: ["useUiStateStore"]
    - path: "src/renderer/src/stores/fileBrowser.ts"
      provides: "Zustand store for file tree, expanded nodes, active path"
      exports: ["useFileBrowserStore"]
    - path: "src/renderer/src/components/Layout.tsx"
      provides: "Main app layout — sidebar + chrome area + viewer panel"
      exports: ["Layout"]
    - path: "src/renderer/src/components/TabBar.tsx"
      provides: "Tab bar with close buttons and drag reorder"
      exports: ["TabBar"]
    - path: "src/renderer/src/components/ViewModeControls.tsx"
      provides: "Segmented control for view mode switching"
      exports: ["ViewModeControls"]
    - path: "src/renderer/src/components/ViewerPanel.tsx"
      provides: "Content area rendering artifact or index placeholder"
      exports: ["ViewerPanel"]
    - path: "src/renderer/src/components/ResizeHandle.tsx"
      provides: "Draggable resize handle for split pane"
      exports: ["ResizeHandle"]
  key_links:
    - from: "src/renderer/src/stores/artifactTabs.ts"
      to: "src/renderer/src/components/TabBar.tsx"
      via: "Zustand store consumed by TabBar for open tabs list"
      pattern: "useArtifactTabsStore"
    - from: "src/renderer/src/components/ViewerPanel.tsx"
      to: "src/renderer/src/components/ViewModeControls.tsx"
      via: "View mode state shared via Zustand uiState store"
      pattern: "useUiStateStore"
    - from: "src/main/ipc/fs.ts"
      to: "src/shared/validators/fs.ts"
      via: "Zod schema import for param validation"
      pattern: "import.*FSListDirSchema"
    - from: "src/preload/index.ts"
      to: "src/main/ipc/fs.ts"
      via: "ipcRenderer.invoke() calls matching ipcMain.handle() channel names"
      pattern: "fs:"
---

## Phase Goal

**As a** developer, **I want to** build the core artifact viewing layout — tab bar, view mode controls, split pane, and Zustand state management — **so that** Phase 2 Plan 02 can wire in file browsing and artifact viewers.

<objective>
Deliver the tabbed workspace shell: Zustand stores for all Phase 2 state, file system IPC channels, tab bar with close/reorder/overflow, view mode segmented control, side-by-side split pane with resize handle, and the Artifact Index placeholder. After this plan, the app has a structured layout ready for viewer components.

Purpose: Establish the layout architecture and state management so that file browser and artifact viewers (Plan 02) have proper integration points.
Output: Restructured app with tab bar, view mode controls, split pane, Zustand stores, and fs IPC channels.
</objective>

<execution_context>
@/home/burt/code/CUSTOM_AI_Coding_Agent/01_Intro/.opencode/gsd-core/workflows/execute-plan.md
@/home/burt/code/CUSTOM_AI_Coding_Agent/01_Intro/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-artifact-viewing/02-CONTEXT.md
@.planning/phases/02-core-artifact-viewing/02-DISCUSSION-LOG.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Zustand and DnD dependencies, create file system IPC channels with Zod validation</name>
  <files>
    ai-cli-studio/package.json (modify),
    ai-cli-studio/src/shared/validators/fs.ts (create),
    ai-cli-studio/src/shared/validators/fs.test.ts (create),
    ai-cli-studio/src/shared/types/ipc.ts (modify),
    ai-cli-studio/src/shared/types/window.d.ts (modify),
    ai-cli-studio/src/main/ipc/fs.ts (create),
    ai-cli-studio/src/main/ipc/index.ts (modify),
    ai-cli-studio/src/preload/index.ts (modify),
    ai-cli-studio/src/renderer/src/hooks/useElectronAPI.ts (modify)
  </files>
  <read_first>
    @.planning/phases/01-foundation/01-02-PLAN.md (Task 1 — IPC infrastructure pattern)
    @.planning/phases/02-core-artifact-viewing/02-CONTEXT.md (D-16, D-20, Integration Points section)
    Existing files:
    @ai-cli-studio/src/shared/types/ipc.ts
    @ai-cli-studio/src/shared/types/window.d.ts
    @ai-cli-studio/src/main/ipc/index.ts
    @ai-cli-studio/src/preload/index.ts
    @ai-cli-studio/src/renderer/src/hooks/useElectronAPI.ts
    @ai-cli-studio/package.json
  </read_first>
  <action>
    STEP 1: Install new dependencies
    ```bash
    npm install zustand @dnd-kit/core @dnd-kit/sortable
    ```

    STEP 2: Add fs channel constants to `src/shared/types/ipc.ts`
    ```typescript
    export const IPC_CHANNELS = {
      APP_QUIT: 'app:quit',
      APP_GET_VERSION: 'app:getVersion',
      WINDOW_MINIMIZE: 'window:minimize',
      WINDOW_MAXIMIZE: 'window:maximize',
      WINDOW_CLOSE: 'window:close',
      FS_LIST_DIR: 'fs:listDir',
      FS_READ_FILE: 'fs:readFile',
      FS_GET_FILE_INFO: 'fs:getFileInfo',
    } as const
    ```

    STEP 3: Add fs methods to `src/shared/types/window.d.ts`
    ```typescript
    export interface ElectronAPI {
      app: {
        quit: (force?: boolean) => Promise<void>
        getVersion: () => Promise<string>
      }
      window: {
        minimize: () => Promise<void>
        maximize: () => Promise<void>
        close: () => Promise<void>
      }
      fs: {
        listDir: (path: string) => Promise<FsDirEntry[]>
        readFile: (path: string) => Promise<string>
        getFileInfo: (path: string) => Promise<FsFileInfo>
      }
      on: (channel: string, callback: (...args: unknown[]) => void) => () => void
    }

    export interface FsDirEntry {
      name: string
      path: string
      isDirectory: boolean
      isFile: boolean
    }

    export interface FsFileInfo {
      size: number
      isDirectory: boolean
      isFile: boolean
      modifiedAt: number
    }

    declare global {
      interface Window {
        electronAPI: ElectronAPI
      }
    }
    ```

    STEP 4: Create `src/shared/validators/fs.ts` with Zod schemas:
    ```typescript
    import { z } from 'zod/v4'

    export const FSListDirSchema = z.object({
      path: z.string().min(1),
    })

    export const FSReadFileSchema = z.object({
      path: z.string().min(1),
    })

    export const FSGetFileInfoSchema = z.object({
      path: z.string().min(1),
    })

    export const FsDirEntrySchema = z.object({
      name: z.string(),
      path: z.string(),
      isDirectory: z.boolean(),
      isFile: z.boolean(),
    })

    export const FsFileInfoSchema = z.object({
      size: z.number(),
      isDirectory: z.boolean(),
      isFile: z.boolean(),
      modifiedAt: z.number(),
    })
    ```

    STEP 5: Create `src/main/ipc/fs.ts` — file system IPC handlers:
    ```typescript
    import { ipcMain } from 'electron'
    import { readdir, readFile, stat } from 'node:fs/promises'
    import { join } from 'node:path'
    import { IPC_CHANNELS } from '../../shared/types/ipc'
    import {
      FSListDirSchema,
      FSReadFileSchema,
      FSGetFileInfoSchema,
    } from '../../shared/validators/fs'

    export function registerFSIPC(): void {
      ipcMain.handle(IPC_CHANNELS.FS_LIST_DIR, async (_event, payload) => {
        const { path } = FSListDirSchema.parse(payload)
        const entries = await readdir(path, { withFileTypes: true })
        return entries.map((entry) => ({
          name: entry.name,
          path: join(path, entry.name),
          isDirectory: entry.isDirectory(),
          isFile: entry.isFile(),
        }))
      })

      ipcMain.handle(IPC_CHANNELS.FS_READ_FILE, async (_event, payload) => {
        const { path } = FSReadFileSchema.parse(payload)
        return await readFile(path, 'utf-8')
      })

      ipcMain.handle(IPC_CHANNELS.FS_GET_FILE_INFO, async (_event, payload) => {
        const { path } = FSGetFileInfoSchema.parse(payload)
        const stats = await stat(path)
        return {
          size: stats.size,
          isDirectory: stats.isDirectory(),
          isFile: stats.isFile(),
          modifiedAt: stats.mtimeMs,
        }
      })
    }
    ```

    STEP 6: Update `src/main/ipc/index.ts` — register fs IPC:
    ```typescript
    import { registerAppIPC } from './app'
    import { registerWindowIPC } from './window'
    import { registerFSIPC } from './fs'

    export function registerAllIPC(): void {
      registerAppIPC()
      registerWindowIPC()
      registerFSIPC()
    }
    ```

    STEP 7: Update `src/preload/index.ts` — add fs methods:
    ```typescript
    import { contextBridge, ipcRenderer } from 'electron'

    const electronAPI = {
      app: {
        quit: (force?: boolean) => ipcRenderer.invoke('app:quit', { force }),
        getVersion: () => ipcRenderer.invoke('app:getVersion'),
      },
      window: {
        minimize: () => ipcRenderer.invoke('window:minimize'),
        maximize: () => ipcRenderer.invoke('window:maximize'),
        close: () => ipcRenderer.invoke('window:close'),
      },
      fs: {
        listDir: (path: string) => ipcRenderer.invoke('fs:listDir', { path }),
        readFile: (path: string) => ipcRenderer.invoke('fs:readFile', { path }),
        getFileInfo: (path: string) => ipcRenderer.invoke('fs:getFileInfo', { path }),
      },
      on: (channel: string, callback: (...args: unknown[]) => void) => {
        const subscription = (_event: Electron.IpcRendererEvent, ...args: unknown[]) =>
          callback(...args)
        ipcRenderer.on(channel, subscription)
        return () => {
          ipcRenderer.removeListener(channel, subscription)
        }
      },
    }

    contextBridge.exposeInMainWorld('electronAPI', electronAPI)
    ```

    STEP 8: Create contract test `src/shared/validators/fs.test.ts` for fs IPC:
    - Test that FSListDirSchema accepts a valid path object
    - Test that FSListDirSchema rejects missing path
    - Test that FSReadFileSchema accepts a valid path object
    - Test that FSReadFileSchema rejects empty string path
    - Test that FSGetFileInfoSchema accepts a valid path object

    STEP 9: Update `src/renderer/src/hooks/useElectronAPI.ts` — no changes needed for the hook itself (it already returns window.electronAPI), but add a note that `useElectronAPI().fs` is now available.

    STEP 10: Build and test:
    - `npm run build` — verify main/preload/renderer compile
    - `npx vitest run` — all existing tests plus new fs tests pass
  </action>
  <acceptance_criteria>
    - `npx vitest run` passes — all existing tests + 5 new fs contract tests pass
    - `npm run build` compiles main + preload + renderer without errors
    - `src/shared/types/ipc.ts` exports IPC_CHANNELS with FS_LIST_DIR, FS_READ_FILE, FS_GET_FILE_INFO
    - `src/shared/types/window.d.ts` exports FsDirEntry, FsFileInfo interfaces and fs namespace on ElectronAPI
    - `src/main/ipc/fs.ts` exports registerFSIPC with 3 handlers
    - `src/main/ipc/index.ts` calls registerFSIPC
    - `src/preload/index.ts` exposes fs.listDir, fs.readFile, fs.getFileInfo
    - package.json includes zustand, @dnd-kit/core, @dnd-kit/sortable
  </acceptance_criteria>
  <verify>
    <automated>cd ai-cli-studio && npx vitest run --reporter=dot 2>&1 && npm run build 2>&1 | tail -5; echo "EXIT_CODE: $?"</automated>
  </verify>
  <done>
    - 3 fs IPC channels implemented with Zod validation (listDir, readFile, getFileInfo)
    - Preload bridge exposes fs namespace to renderer
    - Type declarations updated with FsDirEntry, FsFileInfo interfaces
    - Zustand and DnD dependencies installed
    - All 5 contract tests pass for fs validators
    - Full build compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand stores for artifact tabs, UI state, and file browser</name>
  <files>
    ai-cli-studio/src/renderer/src/stores/artifactTabs.ts (create),
    ai-cli-studio/src/renderer/src/stores/uiState.ts (create),
    ai-cli-studio/src/renderer/src/stores/fileBrowser.ts (create),
    ai-cli-studio/src/renderer/src/stores/index.ts (create)
  </files>
  <read_first>
    @.planning/phases/02-core-artifact-viewing/02-CONTEXT.md (D-21 — Zustand stores, D-08 — 5-tab limit, D-01 — view mode, D-11/D-12 — file browser)
    @.planning/phases/02-core-artifact-viewing/02-DISCUSSION-LOG.md (View Mode Switching, Tab Bar Behavior, File Browser Structure sections)
  </read_first>
  <action>
    STEP 1: Create `src/renderer/src/stores/artifactTabs.ts`:
    ```typescript
    import { create } from 'zustand'

    export interface ArtifactTab {
      id: string
      title: string
      filePath: string
      type: 'code' | 'markdown' | 'image' | 'html' | 'pdf' | 'svg'
      content: string | null
      isLoading: boolean
    }

    const MAX_TABS = 5

    interface ArtifactTabsState {
      tabs: ArtifactTab[]
      activeTabId: string | null
      openTab: (tab: ArtifactTab) => void
      closeTab: (id: string) => void
      setActiveTab: (id: string) => void
      reorderTabs: (fromIndex: number, toIndex: number) => void
      setTabContent: (id: string, content: string) => void
      setTabLoading: (id: string, isLoading: boolean) => void
      hasReachedLimit: () => boolean
    }

    export const useArtifactTabsStore = create<ArtifactTabsState>((set, get) => ({
      tabs: [],
      activeTabId: null,

      openTab: (tab) =>
        set((state) => {
          const existing = state.tabs.find((t) => t.id === tab.id)
          if (existing) {
            return { activeTabId: tab.id }
          }
          if (state.tabs.length >= MAX_TABS) {
            return state
          }
          return {
            tabs: [...state.tabs, tab],
            activeTabId: tab.id,
          }
        }),

      closeTab: (id) =>
        set((state) => {
          const filtered = state.tabs.filter((t) => t.id !== id)
          let nextActive = state.activeTabId
          if (state.activeTabId === id) {
            const closedIndex = state.tabs.findIndex((t) => t.id === id)
            nextActive = filtered[Math.min(closedIndex, filtered.length - 1)]?.id ?? null
          }
          return { tabs: filtered, activeTabId: nextActive }
        }),

      setActiveTab: (id) => set({ activeTabId: id }),

      reorderTabs: (fromIndex, toIndex) =>
        set((state) => {
          const tabs = [...state.tabs]
          const [moved] = tabs.splice(fromIndex, 1)
          tabs.splice(toIndex, 0, moved)
          return { tabs }
        }),

      setTabContent: (id, content) =>
        set((state) => ({
          tabs: state.tabs.map((t) => (t.id === id ? { ...t, content, isLoading: false } : t)),
        })),

      setTabLoading: (id, isLoading) =>
        set((state) => ({
          tabs: state.tabs.map((t) => (t.id === id ? { ...t, isLoading } : t)),
        })),

      hasReachedLimit: () => get().tabs.length >= MAX_TABS,
    }))
    ```

    STEP 2: Create `src/renderer/src/stores/uiState.ts`:
    ```typescript
    import { create } from 'zustand'

    export type ViewMode = 'preview' | 'code' | 'side-by-side'

    interface UiState {
      viewMode: ViewMode
      splitPosition: number // percentage (0-100), default 50
      setViewMode: (mode: ViewMode) => void
      setSplitPosition: (position: number) => void
    }

    export const useUiStateStore = create<UiState>((set) => ({
      viewMode: 'preview',
      splitPosition: 50,

      setViewMode: (mode) => set({ viewMode: mode }),

      setSplitPosition: (position) =>
        set({ splitPosition: Math.max(10, Math.min(90, position)) }),
    }))
    ```

    STEP 3: Create `src/renderer/src/stores/fileBrowser.ts`:
    ```typescript
    import { create } from 'zustand'

    export interface FileTreeEntry {
      name: string
      path: string
      isDirectory: boolean
      isFile: boolean
      children?: FileTreeEntry[]
    }

    interface FileBrowserState {
      rootPath: string | null
      expandedPaths: Set<string>
      activeFilePath: string | null
      tree: FileTreeEntry[]
      isLoading: boolean
      setRootPath: (path: string) => void
      setTree: (tree: FileTreeEntry[]) => void
      toggleExpand: (path: string) => void
      setActiveFile: (path: string | null) => void
      setLoading: (loading: boolean) => void
      isExpanded: (path: string) => boolean
    }

    export const useFileBrowserStore = create<FileBrowserState>((set, get) => ({
      rootPath: null,
      expandedPaths: new Set(),
      activeFilePath: null,
      tree: [],
      isLoading: false,

      setRootPath: (path) => set({ rootPath: path }),

      setTree: (tree) => set({ tree }),

      toggleExpand: (path) =>
        set((state) => {
          const next = new Set(state.expandedPaths)
          if (next.has(path)) {
            next.delete(path)
          } else {
            next.add(path)
          }
          return { expandedPaths: next }
        }),

      setActiveFile: (path) => set({ activeFilePath: path }),

      setLoading: (loading) => set({ isLoading: loading }),

      isExpanded: (path) => get().expandedPaths.has(path),
    }))
    ```

    STEP 4: Create `src/renderer/src/stores/index.ts` — barrel export:
    ```typescript
    export { useArtifactTabsStore } from './artifactTabs'
    export type { ArtifactTab } from './artifactTabs'
    export { useUiStateStore } from './uiState'
    export type { ViewMode } from './uiState'
    export { useFileBrowserStore } from './fileBrowser'
    export type { FileTreeEntry } from './fileBrowser'
    ```

    STEP 5: Verify with build:
    - `npm run build` compiles without errors
    - No test needed for pure Zustand stores (they're data logic, tested via integration)
  </action>
  <acceptance_criteria>
    - 4 store files exist at src/renderer/src/stores/
    - `useArtifactTabsStore` has: tabs, activeTabId, openTab, closeTab, setActiveTab, reorderTabs, setTabContent, setTabLoading, hasReachedLimit
    - `useUiStateStore` has: viewMode, splitPosition, setViewMode, setSplitPosition
    - `useFileBrowserStore` has: rootPath, expandedPaths, activeFilePath, tree, isLoading, toggleExpand, setActiveFile
    - `npm run build` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>cd ai-cli-studio && npm run build 2>&1 | tail -5; echo "EXIT_CODE: $?"</automated>
  </verify>
  <done>
    - 3 Zustand stores created: artifactTabs, uiState, fileBrowser
    - Barrel export at stores/index.ts
    - All store interfaces match D-21 requirements
    - Build compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 3: Build app layout — TabBar, ViewModeControls, ViewerPanel, ResizeHandle, and Layout shell</name>
  <files>
    ai-cli-studio/src/renderer/src/components/TabBar.tsx (create),
    ai-cli-studio/src/renderer/src/components/ViewModeControls.tsx (create),
    ai-cli-studio/src/renderer/src/components/ViewerPanel.tsx (create),
    ai-cli-studio/src/renderer/src/components/ResizeHandle.tsx (create),
    ai-cli-studio/src/renderer/src/components/Layout.tsx (create),
    ai-cli-studio/src/renderer/src/App.tsx (modify),
    ai-cli-studio/src/renderer/src/assets/styles/globals.css (modify — add layout styles)
  </files>
  <read_first>
    @.planning/phases/02-core-artifact-viewing/02-CONTEXT.md (D-01 through D-10, Integration Points for App.tsx)
    @.planning/phases/02-core-artifact-viewing/02-DISCUSSION-LOG.md (View Mode Switching, Tab Bar Behavior sections)
    @.planning/phases/01-foundation/01-UI-SPEC.md (Design System — colors, typography, spacing)
    Existing files:
    @ai-cli-studio/src/renderer/src/App.tsx
    @ai-cli-studio/src/renderer/src/components/WelcomeScreen.tsx
  </read_first>
  <action>
    Create the layout components that form the core workspace shell.

    STEP 1: Create `src/renderer/src/components/ResizeHandle.tsx`:
    A draggable vertical divider for the split pane. Renders as a 4px-wide vertical bar that handles mouse drag to update split position.
    ```typescript
    import { type FC, useCallback, useEffect, useRef } from 'react'
    import { useUiStateStore } from '../stores/uiState'

    interface ResizeHandleProps {
      containerRef: React.RefObject<HTMLDivElement | null>
    }

    export const ResizeHandle: FC<ResizeHandleProps> = ({ containerRef }) => {
      const setSplitPosition = useUiStateStore((s) => s.setSplitPosition)
      const isDragging = useRef(false)

      const handleMouseDown = useCallback(() => {
        isDragging.current = true
        document.body.style.cursor = 'col-resize'
        document.body.style.userSelect = 'none'
      }, [])

      useEffect(() => {
        const handleMouseMove = (e: MouseEvent) => {
          if (!isDragging.current || !containerRef.current) return
          const rect = containerRef.current.getBoundingClientRect()
          const percentage = ((e.clientX - rect.left) / rect.width) * 100
          setSplitPosition(percentage)
        }

        const handleMouseUp = () => {
          isDragging.current = false
          document.body.style.cursor = ''
          document.body.style.userSelect = ''
        }

        document.addEventListener('mousemove', handleMouseMove)
        document.addEventListener('mouseup', handleMouseUp)
        return () => {
          document.removeEventListener('mousemove', handleMouseMove)
          document.removeEventListener('mouseup', handleMouseUp)
        }
      }, [containerRef, setSplitPosition])

      return (
        <div
          onMouseDown={handleMouseDown}
          className="w-1 cursor-col-resize bg-border-default hover:bg-border-accent
                     transition-colors duration-150 shrink-0"
        />
      )
    }
    ```

    STEP 2: Create `src/renderer/src/components/ViewModeControls.tsx`:
    Segmented control with 3 buttons: Preview, Code, Side-by-side. Uses lucide icons.
    ```typescript
    import { type FC } from 'react'
    import { Eye, Code2, Columns2 } from 'lucide-react'
    import { useUiStateStore, type ViewMode } from '../stores/uiState'

    const modes: { value: ViewMode; label: string; Icon: FC<{ size?: number }> }[] = [
      { value: 'preview', label: 'Preview', Icon: Eye },
      { value: 'code', label: 'Code', Icon: Code2 },
      { value: 'side-by-side', label: 'Split', Icon: Columns2 },
    ]

    export const ViewModeControls: FC = () => {
      const viewMode = useUiStateStore((s) => s.viewMode)
      const setViewMode = useUiStateStore((s) => s.setViewMode)

      return (
        <div className="flex items-center gap-0.5 rounded-md bg-bg-secondary p-0.5">
          {modes.map(({ value, label, Icon }) => (
            <button
              key={value}
              onClick={() => setViewMode(value)}
              aria-label={label}
              aria-pressed={viewMode === value}
              className={`
                flex items-center gap-1.5 px-2.5 py-1 rounded text-xs font-medium
                transition-colors duration-150
                ${
                  viewMode === value
                    ? 'bg-bg-elevated text-text-primary'
                    : 'text-text-secondary hover:text-text-primary'
                }
              `}
            >
              <Icon size={14} />
              <span>{label}</span>
            </button>
          ))}
        </div>
      )
    }
    ```

    STEP 3: Create `src/renderer/src/components/TabBar.tsx` with drag reorder and overflow dropdown:
    ```typescript
    import { type FC, useRef, useState, useCallback } from 'react'
    import { X, ChevronDown } from 'lucide-react'
    import {
      DndContext,
      closestCenter,
      type DragEndEvent,
      PointerSensor,
      useSensor,
      useSensors,
    } from '@dnd-kit/core'
    import {
      SortableContext,
      horizontalListSortingStrategy,
      useSortable,
    } from '@dnd-kit/sortable'
    import { CSS } from '@dnd-kit/utilities'
    import { useArtifactTabsStore } from '../stores/artifactTabs'

    interface SortableTabProps {
      id: string
      title: string
      isActive: boolean
      onSelect: () => void
      onClose: () => void
    }

    const SortableTab: FC<SortableTabProps> = ({ id, title, isActive, onSelect, onClose }) => {
      const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id })

      const style = {
        transform: CSS.Transform.toString(transform),
        transition,
        opacity: isDragging ? 0.5 : 1,
      }

      return (
        <div
          ref={setNodeRef}
          style={style}
          {...attributes}
          {...listeners}
          role="tab"
          aria-selected={isActive}
          onClick={onSelect}
          className={`
            group flex items-center gap-1.5 px-3 py-1.5 text-xs cursor-pointer
            border-r border-border-default shrink-0
            transition-colors duration-150 select-none
            min-w-0 max-w-40
            ${
              isActive
                ? 'bg-bg-elevated text-text-primary border-b-2 border-b-accent-primary'
                : 'bg-bg-secondary text-text-secondary hover:text-text-primary hover:bg-bg-elevated/50'
            }
          `}
        >
          <span className="truncate flex-1">{title}</span>
          <button
            onClick={(e) => {
              e.stopPropagation()
              onClose()
            }}
            aria-label={`Close ${title}`}
            className="shrink-0 opacity-0 group-hover:opacity-100 transition-opacity
                       rounded p-0.5 hover:bg-bg-primary hover:text-text-primary"
          >
            <X size={12} />
          </button>
        </div>
      )
    }

    export const TabBar: FC = () => {
      const tabs = useArtifactTabsStore((s) => s.tabs)
      const activeTabId = useArtifactTabsStore((s) => s.activeTabId)
      const setActiveTab = useArtifactTabsStore((s) => s.setActiveTab)
      const closeTab = useArtifactTabsStore((s) => s.closeTab)
      const reorderTabs = useArtifactTabsStore((s) => s.reorderTabs)
      const [showOverflow, setShowOverflow] = useState(false)
      const overflowRef = useRef<HTMLDivElement>(null)
      const barRef = useRef<HTMLDivElement>(null)

      const sensors = useSensors(
        useSensor(PointerSensor, { activationConstraint: { distance: 5 } })
      )

      const handleDragEnd = useCallback(
        (event: DragEndEvent) => {
          const { active, over } = event
          if (!over || active.id === over.id) return
          const fromIndex = tabs.findIndex((t) => t.id === active.id)
          const toIndex = tabs.findIndex((t) => t.id === over.id)
          if (fromIndex !== -1 && toIndex !== -1) {
            reorderTabs(fromIndex, toIndex)
          }
        },
        [tabs, reorderTabs]
      )

      const visibleTabs = tabs
      const overflowTabs = tabs.length > 5 ? tabs.slice(5) : []

      return (
        <div
          ref={barRef}
          className="flex items-center h-9 bg-bg-secondary border-b border-border-default overflow-hidden"
        >
          <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
            <SortableContext
              items={visibleTabs.map((t) => t.id)}
              strategy={horizontalListSortingStrategy}
            >
              <div className="flex-1 flex overflow-hidden">
                {visibleTabs.slice(0, 5).map((tab) => (
                  <SortableTab
                    key={tab.id}
                    id={tab.id}
                    title={tab.title}
                    isActive={tab.id === activeTabId}
                    onSelect={() => setActiveTab(tab.id)}
                    onClose={() => closeTab(tab.id)}
                  />
                ))}
                {overflowTabs.length > 0 && (
                  <div ref={overflowRef} className="relative">
                    <button
                      onClick={() => setShowOverflow(!showOverflow)}
                      className="flex items-center px-2 h-full text-text-secondary hover:text-text-primary transition-colors"
                      aria-label="More tabs"
                    >
                      <ChevronDown size={14} />
                    </button>
                    {showOverflow && (
                      <div className="absolute top-full right-0 mt-1 bg-bg-secondary border border-border-default rounded-md shadow-lg z-50 min-w-32">
                        {overflowTabs.map((tab) => (
                          <button
                            key={tab.id}
                            onClick={() => {
                              setActiveTab(tab.id)
                              setShowOverflow(false)
                            }}
                            className="block w-full text-left px-3 py-1.5 text-xs text-text-secondary hover:text-text-primary hover:bg-bg-elevated transition-colors"
                          >
                            {tab.title}
                          </button>
                        ))}
                      </div>
                    )}
                  </div>
                )}
              </div>
            </SortableContext>
          </DndContext>
        </div>
      )
    }
    ```

    STEP 4: Create `src/renderer/src/components/ViewerPanel.tsx`:
    The content area that renders the active artifact or the index placeholder. Supports split pane in side-by-side mode.
    ```typescript
    import { type FC, useRef } from 'react'
    import { useArtifactTabsStore } from '../stores/artifactTabs'
    import { useUiStateStore } from '../stores/uiState'
    import { ResizeHandle } from './ResizeHandle'
    import { CodeSkeleton, MarkdownSkeleton } from './ui/Skeleton'

    const ArtifactIndex: FC = () => (
      <div className="flex flex-col items-center justify-center h-full text-text-faint">
        <div className="w-16 h-16 rounded-xl bg-accent-primary/10 flex items-center justify-center mb-4">
          <span className="text-accent-primary text-2xl font-bold">AI</span>
        </div>
        <h2 className="text-lg font-medium text-text-secondary mb-1">No artifact open</h2>
        <p className="text-sm text-text-faint">Browse files to open an artifact, or connect an agent</p>
      </div>
    )

    const SkeletonByType: FC<{ type: string }> = ({ type }) => {
      if (type === 'markdown') return <MarkdownSkeleton />
      return <CodeSkeleton />
    }

    export const ViewerPanel: FC = () => {
      const tabs = useArtifactTabsStore((s) => s.tabs)
      const activeTabId = useArtifactTabsStore((s) => s.activeTabId)
      const viewMode = useUiStateStore((s) => s.viewMode)
      const splitPosition = useUiStateStore((s) => s.splitPosition)
      const containerRef = useRef<HTMLDivElement>(null)

      const activeTab = tabs.find((t) => t.id === activeTabId)

      if (!activeTab) {
        return (
          <div ref={containerRef} className="flex-1 overflow-hidden">
            <ArtifactIndex />
          </div>
        )
      }

      const renderPreview = () => {
        if (activeTab.isLoading || !activeTab.content) {
          return <SkeletonByType type={activeTab.type} />
        }
        return (
          <div className="flex items-center justify-center h-full text-text-faint text-sm">
            Preview for {activeTab.type} — viewer coming in Plan 02
          </div>
        )
      }

      const renderCode = () => {
        if (activeTab.isLoading || !activeTab.content) {
          return <SkeletonByType type={activeTab.type} />
        }
        return (
          <pre className="h-full overflow-auto p-4 bg-code-bg text-code-text font-mono text-sm">
            <code>{activeTab.content}</code>
          </pre>
        )
      }

      if (viewMode === 'preview') {
        return (
          <div ref={containerRef} className="flex-1 overflow-hidden">
            {renderPreview()}
          </div>
        )
      }

      if (viewMode === 'code') {
        return (
          <div ref={containerRef} className="flex-1 overflow-hidden">
            {renderCode()}
          </div>
        )
      }

      // side-by-side
      return (
        <div ref={containerRef} className="flex-1 flex overflow-hidden">
          <div className="overflow-hidden" style={{ width: `${splitPosition}%` }}>
            {renderPreview()}
          </div>
          <ResizeHandle containerRef={containerRef} />
          <div className="flex-1 overflow-hidden">
            {renderCode()}
          </div>
        </div>
      )
    }
    ```

    STEP 5: Create `src/renderer/src/components/Layout.tsx`:
    The main app layout: sidebar area (placeholder for file browser), chrome area (tab bar + view mode controls), and viewer panel.
    ```typescript
    import { type FC } from 'react'
    import { TabBar } from './TabBar'
    import { ViewModeControls } from './ViewModeControls'
    import { ViewerPanel } from './ViewerPanel'

    export const Layout: FC = () => {
      return (
        <div className="flex h-screen w-screen bg-bg-primary overflow-hidden">
          {/* Sidebar — file browser placeholder (Plan 02) */}
          <div className="w-56 shrink-0 bg-bg-tertiary border-r border-border-default flex flex-col">
            <div className="p-3 text-xs text-text-faint font-medium border-b border-border-default">
              Files
            </div>
            <div className="flex-1 flex items-center justify-center text-text-faint text-xs">
              File browser — Plan 02
            </div>
          </div>

          {/* Main content area */}
          <div className="flex-1 flex flex-col overflow-hidden">
            {/* Chrome area: tab bar + view mode controls */}
            <div className="flex items-center justify-between bg-bg-secondary border-b border-border-default shrink-0">
              <TabBar />
              <div className="px-2">
                <ViewModeControls />
              </div>
            </div>

            {/* Viewer panel */}
            <ViewerPanel />
          </div>
        </div>
      )
    }
    ```

    STEP 6: Update `src/renderer/src/App.tsx` — replace WelcomeScreen with Layout:
    ```typescript
    import { Layout } from './components/Layout'

    function App(): React.JSX.Element {
      return <Layout />
    }

    export default App
    ```

    STEP 7: Add any needed CSS to `src/renderer/src/assets/styles/globals.css`:
    No extra CSS needed — all styling uses Tailwind utility classes.

    STEP 8: Build and test:
    - `npm run build` — verify all targets compile
    - `npx vitest run` — verify no regressions
    - Manually verify with `npm run dev` that the new layout appears:
      - Sidebar placeholder on the left (56px wide)
      - Tab bar at top (empty — no tabs yet)
      - View mode controls in the top-right
      - Viewer panel shows Artifact Index ("No artifact open")
  </action>
  <acceptance_criteria>
    - `npm run build` compiles all targets without errors
    - `npx vitest run` passes all tests
    - `npm run dev` shows the new layout with sidebar, tab bar, and artifact index placeholder
    - Tab bar renders with drag reorder (dnd-kit) and close buttons
    - View mode segmented control has Preview, Code, Split buttons
    - Side-by-side mode shows preview and code panes with draggable resize handle
    - Artifact Index placeholder displays when no tabs are open
    - All styling uses Tailwind classes from the Phase 1 dark theme palette
  </acceptance_criteria>
  <verify>
    <automated>cd ai-cli-studio && npm run build 2>&1 | tail -10 && npx vitest run --reporter=dot 2>&1; echo "EXIT_CODE: $?"</automated>
  </verify>
  <done>
    - Layout.tsx provides full app layout: sidebar + chrome area + viewer panel
    - TabBar.tsx renders open tabs with close buttons and dnd-kit drag reorder
    - ViewModeControls.tsx provides 3-mode segmented control (Preview/Code/Split)
    - ViewerPanel.tsx renders active artifact content or Artifact Index placeholder
    - ResizeHandle.tsx enables draggable split pane in side-by-side mode
    - App.tsx uses Layout instead of WelcomeScreen (WelcomeScreen preserved for later use)
    - Build compiles cleanly, all tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Preload → Main | File system IPC (fs:listDir, fs:readFile, fs:getFileInfo) reads user's file system |
| File path injection | Malicious artifact content could attempt path traversal via IPC calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-FS | Tampering | Path traversal in fs IPC channels | mitigate | Zod schema validates path is a non-empty string. Main process uses the path directly (no user-controlled prefix) — the renderer only sends paths the file browser constructed from legitimate listDir results. Phase 3+ should add path normalization (path.resolve + prefix check). |
| T-02-02-FS | Information Disclosure | Renderer reads arbitrary files | mitigate | The preload bridge only exposes whitelisted fs methods. The renderer has no direct fs access. Each IPC call requires user interaction (clicking a file in the browser). Phase 3+ will add additional authorization for agent-triggered reads. |
| T-02-03-FS | Denial of Service | Large file reads block main process | accept | Phase 2 uses simple readFile which loads entire file into memory. For files over 1MB, Plan 02 will implement virtualized/windowed rendering. Main process reads are async (non-blocking). |
</threat_model>

<verification>
1. `cd ai-cli-studio && npm run build` succeeds for all three targets
2. `npx vitest run` passes all tests including new fs contract tests
3. `npm run dev` launches app with tabbed layout, view mode controls, sidebar placeholder
4. Tab bar shows close buttons on hover, supports drag reorder
5. View mode segmented control switches between Preview, Code, and Side-by-side
6. Side-by-side mode shows draggable resize handle
7. Artifact Index placeholder displays when no tabs are open
8. 5-tab hard limit enforced by Zustand store (hasReachedLimit)
</verification>

<success_criteria>
- [ ] Layout shell renders with sidebar, tab bar, and viewer panel
- [ ] Tab bar with close buttons, drag reorder, and overflow menu for 5+ tabs
- [ ] View mode segmented control (Preview/Code/Split) switches globally
- [ ] Side-by-side split pane with draggable resize handle at 50/50 default
- [ ] Zustand stores manage artifactTabs, uiState, and fileBrowser
- [ ] IPC channels for fs:listDir, fs:readFile, fs:getFileInfo with Zod validation
- [ ] Artifact Index placeholder shown when no tabs are open
- [ ] Build compiles, all tests pass
- [ ] All STRIDE threats mitigated or accepted with rationale
</success_criteria>

<artifacts_produced>
### Files Created
- `src/main/ipc/fs.ts` — File system IPC handlers (registerFSIPC)
- `src/shared/validators/fs.ts` — Zod schemas for fs IPC (FSListDirSchema, FSReadFileSchema, FSGetFileInfoSchema)
- `src/shared/validators/fs.test.ts` — Contract tests for fs IPC validators
- `src/renderer/src/stores/artifactTabs.ts` — Zustand store for artifact tabs
- `src/renderer/src/stores/uiState.ts` — Zustand store for UI state (viewMode, splitPosition)
- `src/renderer/src/stores/fileBrowser.ts` — Zustand store for file browser state
- `src/renderer/src/stores/index.ts` — Barrel export for all stores
- `src/renderer/src/components/Layout.tsx` — Main app layout component
- `src/renderer/src/components/TabBar.tsx` — Tab bar with dnd-kit drag reorder
- `src/renderer/src/components/ViewModeControls.tsx` — Segmented control for view modes
- `src/renderer/src/components/ViewerPanel.tsx` — Content area with split pane support
- `src/renderer/src/components/ResizeHandle.tsx` — Draggable split pane resize handle

### Files Modified
- `src/shared/types/ipc.ts` — Added FS_LIST_DIR, FS_READ_FILE, FS_GET_FILE_INFO channels
- `src/shared/types/window.d.ts` — Added fs namespace, FsDirEntry, FsFileInfo interfaces
- `src/main/ipc/index.ts` — Added registerFSIPC call
- `src/preload/index.ts` — Added fs methods to electronAPI
- `src/renderer/src/App.tsx` — Replaced WelcomeScreen with Layout
- `package.json` — Added zustand, @dnd-kit/core, @dnd-kit/sortable

### Symbols Defined
- IPC Channels: FS_LIST_DIR, FS_READ_FILE, FS_GET_FILE_INFO
- Types/Interfaces: FsDirEntry, FsFileInfo, ArtifactTab, ViewMode, FileTreeEntry
- Zod Schemas: FSListDirSchema, FSReadFileSchema, FSGetFileInfoSchema, FsDirEntrySchema, FsFileInfoSchema
- Main Process Functions: registerFSIPC
- Zustand Stores: useArtifactTabsStore, useUiStateStore, useFileBrowserStore
- React Components: Layout, TabBar, ViewModeControls, ViewerPanel, ResizeHandle
</artifacts_produced>

<output>
Create `.planning/phases/02-core-artifact-viewing/02-01-SUMMARY.md` when done
</output>
